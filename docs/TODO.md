# DezikLog — бэклог после аудита 10.06.2026

Источник: [FULL-AUDIT-2026-06-10.md](FULL-AUDIT-2026-06-10.md). Все critical/high закрыты 10.06 (коммиты `cfc18e1…f5c2a03`). Здесь — оставшиеся medium/low по приоритету.

## P1 — деньги и риск (ближайший спринт)

- [ ] **SMS-pumping**: включить CAPTCHA в Supabase Auth + серверный rate-limit на номер/IP. Сейчас вход без капчи, кулдаун 5 с на номер — можно жечь SMS-бюджет запросами кодов на случайные номера. `supabase/config.toml:176-196`, `send-sms-hook`
- [ ] **Атомарность заказа**: orders + order_items пишутся двумя insert'ами — обрыв между ними даёт пустой заказ, который синкается в KeyCRM. Перенести в одну RPC-транзакцию. `lib/api.ts:468-513`
- [ ] **Проверить анонимизацию KeyCRM**: после первого реального удаления аккаунта убедиться, что buyer обезличен (имя «Видалений акаунт», телефон/email пустые); при сбое смотреть `admin_alerts` (kind=`buyer_anonymize_failed`). Если KeyCRM отвергает пустые строки — поменять формат PUT в `delete-account/index.ts`

## P2 — магазин

- [ ] **Устаревшая цена в корзине**: цена зафиксирована при добавлении; при открытии корзины/чекаута подтягивать актуальные цены и показывать дельту. `app/cart.tsx:306-308`
- [ ] **Кап количества и остатки**: нет проверки qty vs остаток и нет максимума — возможен oversell. Серверная проверка в триггере + клиентский кап. `20260522_fix_enforce_price_type_cast.sql:38-41`
- [ ] **Отмена заказа в приложении**: сейчас только «Повторити замовлення». Кнопка отмены для статуса pending → KeyCRM. `app/order/[id].tsx:306-327`
- [ ] **Вес ТТН**: захардкожен 1 кг — считать из `products.weight × qty` (колонка уже есть, миграция `20260609000009`). `_shared/sync-logic.ts:300`
- [ ] **Разбивка стоимости в заказе** неверна при платной доставке. `app/order/[id].tsx:259-271`
- [ ] **Reorder молча затирает корзину** — спросить подтверждение. `app/order/[id].tsx:135-140`

## P3 — надёжность и операционка

- [ ] **Crash-телеметрия**: подключить Sentry (`@sentry/react-native`) — сейчас продакшн-краши невидимы, ErrorBoundary без отчётности
- [ ] **CI**: GitHub Actions — `jest` + `tsc --noEmit` на каждый push (тесты сейчас гоняются только руками)
- [ ] **Зависимости**: `npm audit` (20 уязвимостей, 1 critical в дереве); удалить лишние локфайлы `package-lock.json`, `package-lock 3.json`, `metro.config 2.js` — оставить только yarn.lock
- [ ] **Дедуп статус-пушей**: поллер может прислать дубль сразу после вебхука. `poll-keycrm-statuses/index.ts:112-137`
- [ ] **Пуши про растворы игнорируют настройки** уведомлений пользователя + UTC-полночь вместо Киева. `notify-cycle-idle/index.ts:53-54, 85-120`
- [ ] **notify-cycle-idle не зашедулен** — нет cron-миграции (все остальные кроны есть). Заскедулить или удалить
- [ ] **Ротация `KEYCRM_WEBHOOK_SECRET`** (секрет ходит в query string) + перенести в заголовок, когда KeyCRM позволит
- [ ] **Android Auto Backup**: исключить AsyncStorage из бэкапов (`expo-build-properties` → `allowBackup`/`fullBackupContent`)
- [ ] **Чистка temp-файлов**: фото/PDF/сторис-снапшоты копятся в cache-каталоге

## P4 — продукт (из аудита UX)

- [ ] **Поиск в каталоге** (его нет)
- [ ] **SmartSuggestion / дозаказ**: компонент готов (`components/SmartSuggestion.tsx`), данные потребления есть — подключить к главной. Самый недооценённый рост-рычаг по мнению аудита
- [ ] **OTA-канал** (`expo-updates`): сейчас каждый хотфикс = полный цикл App Review
- [ ] Орфан-экран `cabinet/solutions.tsx`, легаси `data/catalog.json`, плюрализация счётчика товаров — мелкая чистка
